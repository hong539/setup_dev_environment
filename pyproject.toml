[project]
name = "setup-dev-environment"
version = "0.1.0"
description = "https://github.com/hong539/setup_dev_environment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.6.14",
]

[dependency-groups]
dev = [
    "boto3>=1.39.12",
]
